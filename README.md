# meet-recap
MeetScribe — a Chrome extension that records Google Meet sessions (tab video + mic audio) and automatically uploads them to a FastAPI backend, which stores the file and extracts audio. No manual downloading or uploading.
